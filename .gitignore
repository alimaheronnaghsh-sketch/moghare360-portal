# Composer dependencies (install at deploy/runtime: composer install)
# Keep composer.json and composer.lock tracked; never commit vendor/.
/vendor/

# MOGHARE360 ERP local/private config
private/erp-config.php
private/m360-otp-config.php
private/m360-online-bridge-config.php
private/production-users.json
private/production-site-config.json
public_html/mirror-config.php
*.local.php
*otp-config.local.php
*.secret.php
.env
.env.*
!.env.example

# Release ZIP binaries (build output — not source truth)
release/*.zip
public_html/release/*.zip
*.zip
!package.zip

# Legacy Codex review extract (reference only)
tools/_legacy_codex_review/

# Runtime storage and backups (never commit)
storage/
private/backups/
private/logs/
deployment/cpanel/**/local-config.php
deployment/cpanel/**/forward-lead.php
runtime/
uploads/
logs/
backups/

# Access-matrix / workshop scanner evidence (must stay out of Git)
tools/_generated/
tools/_tmp_*.php
tools/_tmp_*.txt
tools/access-matrix-route-discovery.json
tools/access-matrix-uat-snapshot.tsv
tools/access-matrix-*.tsv
tools/workshop-*.tsv
tools/workshop-*.txt

# Android / TWA build outputs and secrets (never commit)
*.apk
*.aab
*.ap_
*.keystore
*.jks
**/local.properties
**/signing.properties
**/*.keystore
**/*.jks
android-twa/.gradle/
android-twa/**/build/
android-twa/**/release/
release-artifacts/
release/_artifacts/

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
