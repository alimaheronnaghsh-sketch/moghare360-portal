# Minimal Gradle settings for TWA wrapper (Bubblewrap/Android Studio fill-in)
# Host URL must be HTTPS Production origin — never commit secrets.

org.gradle.jvmargs=-Xmx2g
android.useAndroidX=true
m360.twa.host=PLACEHOLDER_HTTPS_ORIGIN
