# PHASE 12 — Pilot Scenario Model

Tables: `erp_soft_run_pilots`, `erp_soft_run_pilot_scenarios`, `erp_soft_run_pilot_flow_snapshots`.

Flow: customer → vehicle → contract → operation → inventory → finance → CRM → HR.

All data stored in pilot tables; main ERP tables unchanged.
