# MOGHARE360 — Frontend Scaffold (`app/frontend/`)

## Purpose

Future UI components and views for modular ERP. Legacy UI remains in `public_html/` until phased.

## Status

- **Not active runtime**
- Scaffold only — no production activation
- **No direct database write** from UI

## Architecture

**UI → Validation Engine → Workflow Engine → Database → Audit Log**

## Media (Future)

- **Camera direct only**
- **No upload bypass**
