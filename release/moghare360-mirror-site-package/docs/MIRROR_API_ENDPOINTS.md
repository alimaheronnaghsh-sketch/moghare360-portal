# Mirror API Endpoints (Master Server)

| Endpoint | Purpose |
|----------|---------|
| POST /api/customer/request | Customer online request |
| POST /api/auth/staff-login | Staff authentication |
| POST /api/auth/owner-login | Owner authentication |
| POST /api/dashboard/company-owner | Company owner dashboard data |
| POST /api/access/request | User access request |
| POST /api/mirror/health | Mirror health check |

**Note:** If endpoints are not implemented on Master Server yet:
`Master API endpoint implementation required on local server`

Mirror package includes UI + API client scaffold only.
